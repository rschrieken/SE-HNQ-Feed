extends shared/layout.pug

block content
  
  h2 Hot Network Questions
  
  p Questions shown when they are created, their score. The size of the bubble indicates the number of answers.  Hover to see which site, click to visit the question.
  
  canvas(id="myChart" width="400" height="300")
  
  

block script
  
  script(src='https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.3/Chart.min.js') 
  script(src="https://cdn.jsdelivr.net/npm/moment@2.27.0")
  script(src="https://cdn.jsdelivr.net/npm/chartjs-adapter-moment@0.1.1")